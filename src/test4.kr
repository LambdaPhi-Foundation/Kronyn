define greet proc(name) {
    writeln "Hello " .. name
}

@main {
    greet "Brahma"
    set x 0
    
    goto loop
    writeln "done"
}

@loop {
    writeln x
    set x [x + 1]
    if [x < 3] {goto loop}
}